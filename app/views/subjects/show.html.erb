<div class="card my-card">
  <div class="card-body">
    <h2>Nombre de la clase: <%= @subject.name %></h2>
    <h4>Profesor de la clase: <%= Teacher.find(@subject.teacher_id).name %></h4>
    <h4>Hora de la clase: <%= @subject.time %></h4>
    <h4>Nombre del lugar: <%= @subject.room %></h4>
    <h4>Capacidad del lugar: <%= @subject.room_limit %></h4>
  </div>
</div>

  <div><%= link_to "add a student to this class", "/subjects/#{@subject.id}/student_subjects/new" %></div>
  <div><%= link_to "Editar información de esta clase", "/subjects/#{@subject.id}/edit" %></div>
  <%= button_to "Borrar clase", @subject, method: :delete %>


<h4>Listado de estudiantes en esta clase: </h4>

<% @subject.students.each do |student| %>
  <div><%= student.name %></div>
  <div>Edad: <%= student.age %></div>
  <%= button_to "Remove Student from class", 
  "/subjects/#{@subject.id}/delete_by_student/#{student.id}", method: :delete %>
<% end %>